input {
  beats {
    codec => json
    port => 5044
  }
}
filter {
}
output {
  if [action] == "start" {
  	exec {command => "collector -s %{[simulation_id]} start &" }
  }
  
  if [action] == "stop" {
  	exec {command => "collector -s %{[simulation_id]} stop" }
  }
  
  elasticsearch {
    hosts => ["${ELASTICSEARCH}"]
    index => "simulation%{[simulation_id]}"
	document_type => "sim_state"
  }

}
